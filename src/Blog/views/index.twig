{% extends 'layout.twig' %}
{% block title "billet pour alaska : blog" %}
{% block body %}
<h1> Bienvenue sur le blog </h1>

<ul>
  {% for post in posts %}
    <li><a href="{{ path('blog.show', { slug: post.slug, id: post.id }) }}">{{ post.title }}</a></li>
  {% endfor %}
  

</ul>

{% endblock %}
