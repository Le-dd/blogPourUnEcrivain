{% extends '/Blog/layout.twig' %}
{% block title post.title %}

{% block body %}

<h1>{{post.title}}</h1>

<p class="text-muted">{{ post.dateTime | ago }}</p>

{{post.main | nl2br}}


<h2> Commentaire </h2>
{{ create_com(post.id,post.slug) }}
{{ affiche_com(post.id,post.slug) }}

{% endblock %}
