<p>{{ item.text | raw | nl2br}}</p>
<p>{{ item.createdate| agos}}</p>
<table class="table table-striped">
  <thead>
    <tr>
      <td>Pseudo</td>
      <td>Adresse</td>

    </tr>
    <tbody>

      {% for user in users %}
      <tr>
        <td>{{ user.login }}</td>
        <td>{{ user.email }}</td>

      </tr>
      {% endfor %}
    </tbody>
  </thead>
</table>
{{ paginate(users, routePrefix ~ '.edit') }}
