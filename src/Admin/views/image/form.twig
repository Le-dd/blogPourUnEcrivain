
{% if item.url %}
<img src="{{ imageType(item.url) }}" alt="{{ item.alt | striptags }}" width="100%">
{% endif %}
{{ csrf_input() }}
{{ field('url', item.url, "L'image", {type:'file'})}}
{{ field('title', item.title, "Nom de l'image") }}
{{ field('alt', item.alt, "description", {type:'textarea'})}}
