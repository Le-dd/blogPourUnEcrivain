{% extends '/Blog/layout.twig' %}
{% block title "billet pour alaska : login" %}


{% block body %}

  <form action="{{ path('auth.login')}}" method="post">
    {{ csrf_input() }}
    {{ field('login', null, 'Nom d\'utilisateur') }}
    {{ field('password', null, 'Mot de passe') }}
    <button class="btn btn-primary"> Se connecter </button>
  </form>
   <a class="nav-link" href="{{ path('auth.crud.create') }}">S'inscrire au blog</a>
   <a class="nav-link" href="{{ path('auth.crud.editepass') }}">Mot de passe oublié</a>

{% endblock %}
